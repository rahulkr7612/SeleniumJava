package util;

import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.ui.Select;
import testBase.webTestCase;

public class selectutil extends webTestCase {

/*    public Select select;

    public void dropDownByVisibleText(WebElement dropdownElement , String visibleText)*/
   /* {
        select =new Select(dropdownElement);
        select.selectByVisibleText(dropdownElement);
        select.selectByVisibleText();
    }*/
}
